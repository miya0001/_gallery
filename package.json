{
  "name": "underscore-gallery",
  "dependencies": {},
  "devDependencies": {
    "clean-css-cli": "^4.1.11",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^3.0.0",
    "pump": "^3.0.0",
    "rename": "^1.0.4"
  },
  "scripts": {
    "build": "rm -fr css && mkdir css && cleancss -o css/style.min.css src/style.css && curl https://raw.githubusercontent.com/osvaldasvalutis/imagelightbox.js/master/imagelightbox.js -o src/imagelightbox.js && gulp"
  },
  "license": "GPLv2"
}
